<!DOCTYPE html>
<html lang="en">
  <body>
      <% include('partials/_header') %>
      <h1>URL Database</h1>
      <ul>
        <% for(var i in urls) { %>
          <li><%= i + " URL:  " + urls[i] %></li>
          <link rel="stylesheet" type="text/css" href="">
        <% } %>
      </ul>
  </body>
</html>
