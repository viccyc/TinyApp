<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TinyApp</title>
  </head>
  <body>
      <h1>URL Database</h1>
      <ul>
        <% for(var i in urls) { %>
          <li><%= i + " URL:  " + urls[i] %></li>
          <link rel="stylesheet" type="text/css" href="">
        <% } %>
      </ul>
  </body>
</html>
